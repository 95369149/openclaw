# 2026-03-08 工作日志

## 凌晨巡检 / 每日自我进化
- 03:00 由 cron 触发每日自我进化任务。
- 回顾结果：截至 03:00，task-board 中无 activeTasks，readyTasks 5 项，completedTasks 11 项，blockedTasks 0 项。
- 发现：SOUL.md 存在重复/过时规则；排兵布阵.md 与 openclaw.json 的 Agent 编制仍未完全一致；已产出进化提案并更新 ops/state.json。
- 自动修复：补建今日日志；更新 memory/.abstract 的日期与索引指向。

## 07:00-08:00
- **外部情报获取**：通过 `agent-reach` 获取到 OpenClaw 最新爆发的史诗级安全危机（CVE-2026-25253/27487 等，以及 341 个恶意 Skill 供应链投毒）。向厂长提报了《紧急升级与加固计划》（重点：凭据外置脱敏、Skill 全面审计、SSRF网络收紧）。
- **Skill 安装**：应厂长要求，安装了向阳乔木推荐的高价值 Skill：
  - `defuddle`：干净提取网页正文。
  - `yt-search-download`：YouTube 全站搜索、最高 4K 视频下载及字幕提取。
  - `baoyu-skills`：宝玉的 18 个“内容工厂” Skill 合集（包含信息图生成、推文发布、沉浸翻译等）。
- **阻塞问题**：在安装 `anything-to-notebooklm` 时遇到 Python 版本与 pip 依赖（外置系统包、playwright、fastmcp）相关的冲突。已通过链接 `python3.12` 并设定 `PIP_BREAK_SYSTEM_PACKAGES=1` 绕过环境限制，当前依赖安装任务（session `nova-seaslug`）在后台执行中，尚未达到可用状态，待厂长确认是否继续跟进。

- **厂长新要求**：按 Kitt 自己的判断，把已识别的高价值方向（Skill 分层治理、监工机制、工业视频 Prompt Skill、私有 Skills 清单、参数知识库/A-B 测试）逐项主动执行；减少过程打扰，厂长只看结果。要求系统具备持续进化与迭代能力。

- **会话级新铁律（厂长 11:05 明确）**：本聊天界面内必须长期记住并执行“任务类型 → 最优模型/Agent 路由”原则；允许主动调用更强模型或更合适的 Agent 处理复杂任务（如架构、深度推理、多文件、外链分析），前提是该模型/通道连通、联网正常、不会因轻量模型或不稳定 provider 导致整个会话中断。以后优先保证会话稳定性、结果质量与路由正确性，而不是机械坚持当前模型。
- **路由规则修正（厂长 11:07 进一步澄清）**：模型切换的依据不是轻/重，而是“模型特质 × 擅长领域 × 当前链路稳定性”。以后优先按专长路由：推理型做架构/决策，代码型做开发/重构，多模态做图文视频/长文档，快速型做检索/整理/状态类；成本只是次级因素，不能用“轻模型”这种粗糙分类替代能力判断。
- **厂长确认（13:48）**：已明确授权进入“真正路由模式”——不仅可按模型特性主动调用其他模型/Agent，必要时连当前主模型也允许切换；目标是根据任务类型与模型专长做最优分工，而不是固定守着默认主模型。
- **厂长新偏好（13:53）**：当 jimmy/Kitt 对方向、判断、路由拿不定主意时，允许主动调用 `opus` 作为高阶仲裁/复核模型；适用于方案PK、方向判断、复杂取舍、关键分歧场景。

### 16:30 会话压缩存档
#### 1. 关键决策与原因
- **安装并接入 bibigpt-skill**：补齐音视频（B站/YouTube/播客）一键摘要的默认能力。
- **否决本地部署 ACE-Step 1.5 音乐模型**：笔记本算力难以支撑稳定生产，放入云端/远程算力储备池，主抓轻接入、高价值能力。
- **确立 Opus 为高阶仲裁器**：当方向拿不准、方案难取舍、路由分歧时，主动调用 `opus` 做二次判断定盘。
- **维持 Telegram 主交互，不迁移 Discord**：考量到厂长移动端轻量级决策习惯，通过后台的 task-board 和 shared 隔离信息，而不是换 UI。
- **全量备份并升级 OpenClaw 2026.3.7**：解决明文密钥安全危机，获取 ACP 断点续传和可插拔上下文引擎。
- **电工/钳工培训资料处理标准**：确立结构化（背景-任务-规则）、图文穿插（结合红太阳官网机器图）、标准化落盘 Markdown 的三准则，以适配 NotebookLM。

#### 2. 状态变更
- OpenClaw 系统版本升至 `v2026.3.7`，底层具备断点存活能力。
- 路由规则深度更新落盘：不再被默认主模型绑定，明确根据特质×领域×稳定性动态调度。
- Opus 接口偶发超时，系统已验证稳定性降级机制（切回 Gemini 兜底接管）。

#### 3. 经验教训
- 工具的升级不是堆叠 UI（如 Discord），而是后台对“信息→判断→动作”流水线的编排。
- `web_search` 的中文查询参数必须设为 `zh-hans`（本轮因错误设为 `zh` 导致无效），`web_fetch` 获取外部文章面临被拦截风险，需依赖独立的抓取渠道或跳板。
- 系统文件 Markdown 化及“检索白名单”机制是做大管家的核心，乱喂资料会导致向量库和 NotebookLM 混乱。

#### 4. 阻塞问题
- 当前 `web_fetch` 对部分链接被 Surge 代理或目标网站屏蔽，外部信息长文摄取通道受阻。

#### 5. 厂长的指示
- “很多模型都能用，根据特性调用，做到真正的路由模式，需要时主模型也可切换。”
- “拿不定主意可以让 opus 判断一下。”
- “读取抓到的X内容去迭代电工及钳工培训资料，穿插官网图片，不至于 Notebook 混乱输出。”
