# 2026-03-11 工作日志

## 关键事件

### 1. 记忆整合层方案 v1 落地
- 写了完整方案：`memory/shared/2026-03-11_记忆整合层落地方案_v1.md`
- 核心思路：不推翻现有记忆系统，新增"主动整合层"
- 三层结构：`insights/` + `summaries/` + `consolidation/`
- 子 Agent 精准喂料机制（默认只喂 3 类材料）
- 厂长已确认，Phase 1 待执行（建目录 + 写基础 insight 文件）

### 2. 巡检链路修复
- `ops_hourly_sweep` cron 失败原因：`bin/doubao-healthcheck.py` 路径错误
- 实际文件在 `~/bin/doubao-healthcheck.py`，巡检在找 `workspace/bin/doubao-healthcheck.py`
- 已修复：写了桥接脚本到 `workspace/bin/doubao-healthcheck.py`，验证通过

### 3. Provider 全线故障（重大）
- **mynewapi**（opus/sonnet）：上游 502，完全不可用
- **mygptapi**（gpt-5.4）：401，API Key 失效或余额耗尽
- **google-gemini**（flash）：✅ 唯一正常的 provider
- 这导致厂长从 ~10:00 到 ~17:00 断断续续无法连接（约 6 小时）
- 建议临时切 jimmy 主模型到 gemini-2.5-flash，等 mynewapi 恢复再切回
- 厂长尚未确认是否切换

### 4. 短视频选题推送
- cron `daily_video_topics` 正常产出 3 个选题：
  1. 振动刀精度展示
  2. 汽车内饰一键换装
  3. 客户痛点（拒绝浪费）
- 已推送给厂长

### 5. 钳工培训通知（未完成）
- 厂长 10:03 要求写钳工岗位技能培训通知
- 时间：下午 1:00，参会人员：钳工、质检
- **因 provider 故障未能及时回复，此任务未完成**

### 6. X 链接解读（已完成部分）
- Grace (@Graceruansu)：SOP 规则文件化方法 → 有用，可迁移
- MindfulReturn：OpenClaw Expert Suite（7专家Agent）→ 收藏，不急
- Berryxia：Google Always-On Memory Agent → 高价值，已产出记忆整合层方案
- Ghost OS (@gojun315)：Mac 本地执行层 → 有价值但排第二

## 教训
1. **Provider 单点故障再次导致长时间不可用**（连续第 N 天出现类似问题）
2. **Fallback 链没有正确工作**：opus→sonnet→gpt-5.4 全挂时，应该自动 fallback 到 gemini flash，但实际卡住了
3. **需要在 fallback 链中加入 gemini 作为兜底**，确保至少有一个免费 provider 能接住

## 待办
- [ ] 切 jimmy 主模型到 gemini-2.5-flash（等厂长确认）
- [ ] 检查 mygptapi 余额/key 状态
- [ ] 完成钳工培训通知
- [ ] 执行记忆整合层 Phase 1（建目录 + 写 insight 文件）
- [ ] 修复 fallback 链，确保 gemini flash 作为最终兜底
