# 00_大脑中枢 — Kitt 三层大脑架构 v5.5
> Last Updated: 2026-03-03 18:58
> 核心：三层大脑 + 路由多 agents + 10 个意图桶 + 外部碰撞 + QMD 记忆搜索
> ⚠️ Agent 编制/路由规则/Fallback 链的唯一权威源：`01_强制规则/排兵布阵.md` v6.0
> ⚠️ 产品信息唯一权威源：`02_知识库/红太阳企业信息档案.md`

---

## ⚡ 失忆恢复（第一步必读）
醒来后按顺序：
1. **读本文件**（你正在读）— 理解三层架构和路由规则
2. **读 .abstract** → `memory/.abstract` L0 索引
3. **读今日日志** → `memory/2026-MM-DD.md`（不存在读昨天的）
4. **读 task-board.json** → 当前任务状态
5. **读 shared/** → `ls memory/shared/` 最新 3 条
6. **session_status** 确认当前模型
7. **向厂长汇报**恢复状态

---

## 🧠 三层大脑 + 10 意图桶 + Agent 编制

> **详见唯一权威源：`01_强制规则/排兵布阵.md` v6.0**
> 以下仅为速查摘要，冲突时以排兵布阵为准。
> 最后校准：2026-03-05 22:00（以 openclaw.json 实际配置为准）

- **L0 jimmy**（mynewapi/claude-opus-4-6）→ 调度中枢 + 验收（不接重活）
- **L1 deep**（mygptapi/gpt-5.3-codex）→ 主力，中文/代码/批量
- **L1 main**（google-gemini-cli/gemini-3-pro-preview）→ 多模态/长文档 🆓
- **L1 kitt**（mynewapi/claude-sonnet-4-6-thinking）→ 深度推理+架构决策（合并原logic）💰💰

11 个意图桶（S1/S2/SC1/SC2/IM1/IM2/M1/M2/X1/DEV1/MISC）→ 映射规则见排兵布阵 v8.0。

---

## 🔫 外部碰撞

> 完整规则见 `memory/01_强制规则/外部碰撞规则.md`

**三条铁律**：
1. 碰撞必开**思考模式**
2. 只有 **jimmy** 操作浏览器（防限流）
3. 内部能搞定的**不碰撞**

**工具优先级**: web_search（免费）→ web_fetch（免费）→ agent-browser（免费）→ Perplexity Pro（会员）→ sessions_spawn（内部）

**Perplexity 模型选择**：
| 模型 | 用途 |
|------|------|
| Sonar | 快速搜索、事实查证 |
| GPT-5.2 | 深度推理、复杂方案 |
| Grok 4.1 | 反向验证、独立视角 |
| Claude Sonnet 4.6 | 代码/架构审查 |
| Kimi K2.5 | 中文内容、国内市场 |
| Gemini 3.1 Pro | 长文档、多模态 |

**防限流**: 思考模式 ≤15 次/天，同模型间隔 ≥30s

**其他**: 豆包（`python3 ~/bin/doubao_client.py`）中文润色

---

## 🛠 工具链（2026-03-03 更新）

| 工具 | 用途 | Token 消耗 | 状态 |
|------|------|-----------|------|
| web_search | 网页搜索 | 极低 | ✅ |
| web_fetch | 网页内容提取 | 低 | ✅ |
| agent-browser | AI Agent 浏览器自动化 | 极低（节省95%） | ✅ 已安装 v0.15.2 |
| OpenClaw browser | 浏览器控制（可手动登录） | 中等 | ✅ |
| agent-reach | 多平台内容抓取 | 低 | ✅ |
| Perplexity Pro | 外链浏览器+多模型碰撞 | 中等 | ✅ 通过 browser 工具操作 |

### agent-browser vs OpenClaw browser
- **agent-browser**：Token 节省 95%，但被 X/Twitter 等反爬虫拦截
- **OpenClaw browser**：Token 消耗较高，但可手动登录绕过反爬虫
- **建议**：无反爬虫网站用 agent-browser，需要登录的用 OpenClaw browser

---

## 📁 三层记忆体系

### L0 — 索引层（启动必读）
| 文件 | 内容 |
|------|------|
| `memory/.abstract` | 目录结构 + Agent 编制 + 快速路由 |
| `memory/task-board.json` | 活跃/阻塞/完成任务 |
| `memory/2026-MM-DD.md` | 今日工作日志 |

### L1 — 目录层（按需读取）
| 目录 | 内容 | 优先级 |
|------|------|--------|
| `01_强制规则/` | 安全红线、路由规则、碰撞规则、审批队列 | P0 永不删 |
| `02_知识库/` | 提示词系统、行业知识 | P1 |
| `03_语义记忆/` | 学习笔记、分析报告 | P1 |
| `04_情景记忆/` | 对话记录、场景复盘 | P2 |
| `05_日常日志/` | 每日工作日志（30天后归档）| P1 |
| `06_工具配置/` | Perplexity、工具配置 | P1 |
| `07_版本控制/` | 进化提案、版本日志 | P1 |
| `08_内容素材/` | 朋友圈、文案素材 | P2 |
| `10_项目/` | 进行中项目 | P1 |
| `scripts/` | 自动化脚本 | P0 |

### L2 — 文件层（具体内容）
各目录下的具体 .md 文件，按需读取。

### 公共记忆（多 Agent 共享）
> **详见：`01_强制规则/多Agent协同与公共记忆协议.md`**

| 路径 | 用途 | 规则 |
|------|------|------|
| `memory/shared/` | Agent 间通信 | 完成任务后必须写一条记录 |
| `memory/task-board.json` | 任务状态 | jimmy 独占写入，其他 agent 仅读 |
| `memory/ops/state.json` | 运维状态 | cron 任务写入，心跳读取 |

---

## 💰 API 资源（2026-03-03 18:58 更新）

| Provider | 用途 | 状态 |
|----------|------|------|
| mynewapi | jimmy(opus) + kitt(sonnet-thinking) | ✅ 正常 |
| metaapi | deep(gpt-5.3-codex) | ✅ 正常 |
| xjrouter | fallback（本地代理） | ❌ auth_unavailable |
| google-gemini-cli | main(gemini-3-pro) | ✅ 正常 |
| google-gemini | flash/pro（API Key） | ✅ 正常 |
| groq | qwen3-32b 兜底 | ✅ 正常 |

---

## 🔄 闭环机制

### 任务闭环
1. **接收** → 复述确认，写入 `task-board.json`
2. **路由** → 按意图桶匹配 agent
3. **执行** → agent 产出成果
4. **写入** → 结果写 `shared/`，状态更新 `task-board.json`
5. **审核** → 检查质量，汇报厂长

### 记忆闭环
1. **实时写入** → 完成任务/重要发现/配置变更 → 立即写文件
2. **日志归档** → 每日日志 `memory/2026-MM-DD.md`
3. **备份同步** → launchd 每 30 分钟执行 `sync.sh`（GitHub + iCloud）
4. **定期清理** → `memory-cleanup.sh` 归档过期内容

### 进化闭环
1. **每日 03:00** → 自我进化任务
2. **生成提案** → `07_版本控制/进化提案_YYYY-MM-DD.md`
3. **低风险自动实施，高风险提交审批**
4. **更新版本日志**

---

## 🔐 备份体系

| 位置 | 方式 | 频率 | 状态 |
|------|------|------|------|
| GitHub kitt-brain | sync.sh auto | 每 30 分钟 | ✅ |
| iCloud tar 快照 | sync.sh auto | 每 30 分钟 | ✅ |
| iCloud latest 镜像 | sync.sh mirror-latest | 手动 | ✅ |

## 🧩 记忆系统关键配置

| 配置项 | 值 | 说明 |
|--------|-----|------|
| memory.backend | qmd | QMD 三路混合搜索（BM25+向量+Reranking）|
| memorySearch.provider | local | 本地嵌入模型 |
| compaction.memoryFlush.softThresholdTokens | 40000 | 提前触发记忆刷新 |
| compaction.reserveTokensFloor | 20000 | 压缩保留 |
| experimental.sessionMemory | true | 历史会话可搜索 |

---

## ⚙️ 维护规则

- **配置变更（防漂移铁律）** → 修改 `openclaw.json` 后，必须同步更新：排兵布阵.md + .abstract + 大脑中枢.md
- **Agent 模型变更** → 立即更新编制表 + .abstract + 大脑中枢
- **Provider 状态变化** → 立即更新 Provider 状态表
- **新增工具** → 更新工具链表
- **碰撞规则变更** → 更新 `01_强制规则/外部碰撞规则.md` + SOUL.md

**原则**: 大脑中枢永远是最新的，不是历史记录。
