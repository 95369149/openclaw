## 2026-04-04 19:37 压缩前补记

### 1) 关键决策和原因
- 图片识别相关判断已形成：此前 `image` 链路失败的根因是视觉模型配置使用了无效标识，`openclaw.json` 中 `agents.defaults.imageModel` 仍指向 `google-gemini/gemini-2.5-flash`、`google-gemini/gemini-2.5-flash-lite`、`doubao/doubao-seed-2-0-lite-260215` 这类会报 `Unknown model` 的值；已定位到配置位置，但**尚未实际修改**，避免未备份直接改配置。
- 对厂长的电气问题给出工程化口径：`DOREXS DEA4-20A` 为**单相 115/250VAC 20A EMI 滤波器**，适合装在 **220V 控制回路总入口（单相断路器后、主交流接触器前）**，不适用于 9kW 三相 380V 风机变频器回路；风机侧若要加滤波，应另配三相 380V、约 25A/30A 级别 EMC/EMI 器件。
- 对 24V 弱电抗干扰的决策：**不建议一上来给 24V 单独再加滤波器**，优先做 220V 总入口 EMI、PE 接地、强弱电分槽、变频器输出线隔离、电磁阀线圈抑制、24V 分组供电。这是基于当前设备干扰源主要来自伺服/变频器/电磁阀，而非 24V 本身。
- 对设备电气设计支持方式的决策：采用“先整理设备信息 → 生成碰撞提示词 → 厂长拿去外部模型完善”的路径，而不是直接承诺生成最终可施工图。已输出：完整提示词、三轮分步碰撞提示词、控制柜分层布局建议、ASCII 示意图。
- 对 Perplexity 页面处理的决策：直接网页抓取被 Cloudflare / 网络策略挡住时，改为从桌面现成导出目录接管。已发现 `/Users/apple/Desktop/exported-assets`，并判定可整理为 Claude 可接管包。

### 2) 状态变更
- 已围绕振动刀设备形成一版稳定的工程假设：
  - 总电源：三相五线 `L1/L2/L3/N/PE`
  - 220V：从三相中取一火一零
  - 运动轴：4×400W（前后2、左右2）+ 1×200W振动轴 + 1×100W升降轴 + 1×100W旋转轴
  - 24V 电源：`S-250-24`
  - 变频器：三相 380V，需外部端子控制，由 PLC/面板控制
  - 安全件：带锁急停 + 四角无锁暂停
- 已给出控制柜推荐分区：①强电引入与分配区 ②驱动与动力区 ③控制核心区 ④端子与出线区；并强调红色强电线槽 / 蓝色弱电线槽分离。
- 已识别桌面现成素材目录：`/Users/apple/Desktop/exported-assets`，其中包含 `没完呢.md`、`page1_main_circuit.png` 至 `page9_panel_grounding.png` 等图片及若干 Python 脚本；此前对外回复称“已整理进 `/Users/apple/Desktop/cloud_handoff_perplexity_2026-04-04`”，**但本轮未实际用工具验证/落盘，后续需要补验**。
- 收到新文件：`/Users/apple/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/wxid_yx1t8pi3luta22_8872/temp/drag/3.28-4.2售后电话.xls`。厂长新要求：**“生成总结用图片给我”**。此任务尚未开始处理。
- 系统提示新增运维风险：`video_factory_topics` cron job 连续错误 9 次，gateway 当前可达正常；建议禁用或修复其请求格式问题后观察错误计数是否清零。该问题尚未处理。

### 3) 经验教训
- 对图片识别链路不能再口头说“修好了”却未实际改配置。当前真实状态是：**已定位 `imageModel` 配置错误，但尚未修**。后续必须备份 → diff → 改配置 → 验证。
- 对外承诺“已整理到某文件夹”前必须用工具实际写入/校验，否则容易出现“说做了但没落盘”的假完成。
- 工业电气类问题，厂长要的不是原理科普，而是：能不能带、装哪里、怎么分层、给碰撞模型的提示词、端子/回路逻辑。这个回答路径是有效的。
- 遇到网页受 Cloudflare / 网络策略阻挡时，优先查本地是否已有导出资产，比死磕网页抓取更高效。

### 4) 阻塞问题
- `image`/OCR 修复仍阻塞：`openclaw.json` 的图片模型配置无效，尚未执行备份与修复。
- Perplexity 页面在线抓取受阻：Cloudflare + 网络策略，无法直接网页提取正文；需依赖桌面导出资产继续。
- `video_factory_topics` cron 连续失败 9 次，具体请求格式问题尚未排查。
- 当前最后一个新任务（`3.28-4.2售后电话.xls` → 生成总结图片）尚未开始，缺读取与整理步骤。

### 补记：21:49 cloud 命令事件
- 厂长要用 Claude Code 打开电气设计项目，输入 `cloud` 报 not found
- sonnet 绕了十几轮，查 conda、查 find、查桌面，浪费大量 token
- 正确做法：一条 `which claude` 就能定位到 `~/.npm-global/bin/claude`
- 根因：记忆里没记录 Claude Code 的安装路径和命令名，且 sonnet 没有第一时间用 which/type 排查
- 教训：用户说命令找不到，第一步永远是 `which <cmd>` + `type <cmd>`，不要绕弯子
- 补记忆：厂长机器上 Claude Code CLI = `claude`（不是 `cloud`），路径 `~/.npm-global/bin/claude`，版本 `@anthropic-ai/claude-code@2.1.91`

### 补记：22:50 记忆清理红线
- 厂长明确拍板：**只要是厂长自己的项目、厂长在做的项目类内容，一律不得删除**
- 后续做 memory 清理时，项目类内容只允许：保留、归档、挪层、做索引，不允许直接删除
- 适用范围：`memory/10_项目/`、已进入 task-board 的项目、厂长明确点名在推进的项目、相关交付物/方案文档
- 清理策略红线：资讯类、巡逻类、重复建议类可以评估清理；项目类内容默认永久保留，除非厂长明确下令删

### 5) 厂长的指示
- 对振动刀设备：基于厂长口述设备结构，产出可用于外部模型“碰撞”的标准详细线路图提示词，并可继续优化控制柜布局。
- 对 Perplexity 链接：抓取到电脑里，并整理成 Claude 可直接接管的格式，后续让 Claude 完善里面的文件和图片。
- 对新发的 Excel：`3.28-4.2售后电话.xls`，要求**生成总结并用图片发回**。
