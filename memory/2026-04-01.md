# 2026-04-01 工作日志

## 关键决策与原因

### 1. Kimi 模型 ID 对齐修复
- **问题**：系统别名 `kimi` 指向 `kimi/kimi-for-coding`，但配置注册的是 `kimi/kimi-k2.5`，导致模型切换失败
- **决策**：将两处引用（别名映射 + sino agent 的 primary model）从 `kimi-for-coding` 改为 `kimi-k2.5`
- **原因**：API 端实际模型名是 `kimi-k2.5`，`kimi-for-coding` 只是返回字段，请求时必须用正确的模型 ID

### 2. Claude Code 入口文件创建
- **决策**：为拓客系统创建 `CLAUDE_CODE.md` 作为 Claude Code 的专用入口
- **内容**：包含项目概述、9个已完成修复、已知问题、技术债务、常用命令
- **原因**：让 Claude Code 能快速接手，避免重复劳动和上下文冲突

### 3. 文件打包策略
- **决策**：创建两个包 — 精简版（5个文件）和完整版（122MB）
- **原因**：Claude Code 只需要关键文件做补丁，不需要整个项目

## 状态变更

| 项目 | 之前 | 现在 |
|------|------|------|
| Kimi 模型切换 | 失败（ID不匹配） | ✅ 正常（kimi-k2.5） |
| Gateway | 正常 | ✅ 正常（HTTP 200） |
| Cron Jobs | 部分告警 | ✅ 全部正常（errors < 5） |
| 拓客系统后端 | 运行中 | ✅ 运行中（PID 98764） |
| 拓客系统前端 | 运行中 | ✅ 运行中（PID 98772） |
| Apollo API | 403 失败 | ❌ 仍失败（Key 过期） |
| ImportYeti | 正常 | ⚠️ 新增归零（需扩关键词） |

## 经验教训

1. **模型 ID 必须严格对齐**：provider 配置里的 `id`、系统别名、agent 的 model 字段三者必须一致，否则切换失败
2. **Claude Code 需要结构化入口**：直接丢给它整个项目效率低，用 `CLAUDE_CODE.md` 做导航能大幅提升接手速度
3. **文件打包要分层**：大项目传完整包不现实，关键文件单独打包更实用

## 阻塞问题

- **Apollo API Key 过期**：连续5天 403，需厂长手动更新（https://app.apollo.io/#/settings/integrations/api）
- **ImportYeti 新增归零**：存量线索已覆盖当前关键词，建议扩展：leather cutting machine、fabric cutter、automotive interior cutting

## 厂长的指示

- 对齐 Kimi 模型 ID（已完成）
- 创建 Claude Code 入口文件（已完成）
- 打包文件放桌面（已完成）

---

## 今日完成事项

1. ✅ 修复 Kimi 模型切换问题（ID 对齐）
2. ✅ 创建 `CLAUDE_CODE.md` 入口文件
3. ✅ 打包关键文件到桌面
4. ✅ 启动拓客系统（前后端）
5. ✅ 回应 Claude Code 的文件请求

## 待办（厂长决策）

1. 更新 Apollo API Key（恢复线索采集）
2. 决策是否扩展 ImportYeti 关键词
3. 决策是否配置自动备份 cron 任务

---
*写入时间：2026-04-01 19:59 GMT+8*

## 补充记忆（20:04）

### 状态变更
- 厂长晚间连续追问当前模型与是否卡死，说明此前 flush 回包格式不对，用户侧感知为助手没正常说话。
- 用户确认关心点：
  1. 当前到底是什么模型
  2. 微信渠道是否支持语音
  3. 助手是否卡死

### 经验教训
1. **遇到 flush 指令时，最终对用户只能回 `NO_REPLY`**，不要夹带“当前模型是...”之类正常答复，否则容易让厂长看到异常输出，误以为系统卡住或乱回。
2. **微信语音能力边界要说清**：当前会话里只能处理文字，不能直接听微信语音；这类能力边界应明确、一次说透。
3. **用户问“什么模型”这类直接问题时，正常轮次要直接答具体模型名，不要被压缩提示打断成奇怪格式。**

### 厂长的指示
- 追问“什么模型？”
- 追问“你听不了语音？”
- 追问“卡死？”

### 待注意问题
- 后续若再次遇到上下文压缩提示与用户真实问题交错，优先完成 memory flush，然后严格只回 `NO_REPLY`，避免混合响应。
