## 2026-04-09 07:07 CST

### 关键决策和原因
- 厂长新增明确任务：去 `skills.sh` 专门筛一轮，找出最值得接入当前体系的 10 个 skill，并同步判断现有 skill 中哪些存在更优外部参考，应该升级或替换。
- 已形成一版筛选结论（尚未正式落盘到独立 shared 文件）：优先不是“全装”，而是区分“直接接入”与“吸收结构升级本地 skill”。原因：避免把系统做成 skill 拼装市场，保持我们自己的体系完整。
- 形成的 Top 10 候选清单：
  1. `inferen-sh/skills/ai-rag-pipeline`
  2. `inferen-sh/skills/agent-browser`
  3. `yofine/skills/blueprinter`
  4. `inferen-sh/skills/data-visualization`
  5. `inferen-sh/skills/competitor-teardown`
  6. `inferen-sh/skills/technical-blog-writing`
  7. `inferen-sh/skills/remotion-render`
  8. `inferen-sh/skills/twitter-automation`
  9. `inferen-sh/skills/web-search`
  10. `inferen-sh/skills/python-executor`
- 形成的优先级判断：
  - 第一梯队（优先吸收）：`ai-rag-pipeline`、`agent-browser`、`blueprinter`、`data-visualization`、`competitor-teardown`
  - 第二梯队：`technical-blog-writing`、`remotion-render`、`twitter-automation`
  - 第三梯队：`web-search`、`python-executor`
- 形成的核心结构判断：很多外部 skill 的价值在“结构/方法论”，不在“原样安装”。应优先吸收进本地 skill 体系。

### 状态变更
- 已给出对现有本地 skill 的升级判断（尚未改文件、未提交代码）：
  - `baoyu-infographic`：应升级，吸收 `blueprinter + data-visualization`，拆成 Blueprint 模式 / Viz 模式。
  - `slides` / `Skywork-ppt`：应升级，避免花哨 PPT 风，统一按工程图 + 数据图 + 技术写作结构输出。
  - `biz-reporter`：应升级，吸收 `competitor-teardown + ai-rag-pipeline`，补 claim/evidence/contradiction。
  - `x-reader` / `x-writer`：应升级但不宜立刻替换；可参考 `twitter-automation` 的动作分层与失败降级。
  - `doc` / `baoyu-format-markdown`：应升级，吸收 `technical-blog-writing` 的固定模板（教程/架构/复盘/benchmark/决策备忘录）。
  - `video-factory` / `jianying-video-gen`：不替换，补一条 `remotion-render` 的可控工程视频分支。
- 已形成下一步建议的三件优先动作：
  1. 升级 `learning_ingest_batch`：吸收 `ai-rag-pipeline`，从“扫 inbox 写摘要”升级成 claim/evidence/wiki compile 流。
  2. 升级图形表达体系：吸收 `blueprinter + data-visualization`，形成统一架构图/流程图/数据图规范。
  3. 升级 `biz-reporter`：吸收 `competitor-teardown + technical-blog-writing`，让竞品/行业/方案报告结构化。

### 经验教训
- `skills.sh` 这类站点真正有价值的不是“多”，而是提供成熟的能力结构、输入输出约束和工作流设计；应优先吸收结构，少做原样拼装。
- 对外部 skill 的评估应分两步：
  1. 是否值得接入能力层；
  2. 是否值得直接替换本地 skill。两者不能混为一谈。
- 当前我们更需要“升级本地 skill 的骨架”，而不是新增一堆并列 skill，避免体系继续膨胀。

### 阻塞问题
- 本轮筛选结论目前只在对话中，尚未正式写成 `memory/shared/` 独立清单文件，也尚未进入代码/配置改造阶段。
- 尚未实际逐个读取/安装/验收这 10 个 skill 的完整内容与兼容性；当前是基于已知页面、定位和方法价值做的体系筛选结论。

### 厂长的指示
- 明确要求：`去 skills.sh 上专门筛一轮，找出最值得接到咱们体系里的 10 个 skill 顺带着咱现有的有比咱更优秀的skill去升级替换`
- 偏好仍然不变：先做、直接推进、不要卡住、不要把动作甩回厂长。

## 2026-04-09 07:55 CST

### 关键决策和原因
- 将“减少系统噪音”和“把 ingest 从摘要流扳到编译流”视为同一工程单元，一次性改动 `/Users/apple/.openclaw/cron/jobs.json` 的两条关键 job：`dream_memory_consolidation` 与 `learning_ingest_batch`。原因：这两者共享同一症状——旧 prompt 把输出当成交付，而不是把结构化知识当成交付。
- 对 `dream_memory_consolidation` 采用最小可逆改动：不动调度频率、不动 job id、不动脚本路径，只改 prompt 中的完成条件，要求“正常完成静默，只有异常才输出”。原因：先止血噪音，再决定是否重写 `bin/dream.py`。
- 对 `learning_ingest_batch` 采用 prompt 级架构扳正，而不是直接重写脚本/CLI。原因：当前目标是先把 cron 的行为边界从“手写 wiki 文件”修正到“claim/evidence/source/topic/tags/contradiction 的结构化编译输入”，先纠正方向，再做自动 compile 的代码化。
- 先写备份再改配置，备份路径为 `/Users/apple/.openclaw/cron/jobs.json.bak.20260409_074602`。原因：外部运行配置不在 workspace git 里，回滚必须一条命令可做。

### 状态变更
- 已修改 `/Users/apple/.openclaw/cron/jobs.json`：
  - `dream_memory_consolidation` 的 payload 已改为：正常完成不输出、不再回复 `Dream完成`、仅异常输出简短摘要。
  - `learning_ingest_batch` 的 payload 已改为 `memory-wiki bridge 版`，要求产出 claim/evidence/source/tags/topic/contradiction，并明确 shared 只保留 digest。
- 已完成现场验收：再次读取 `jobs.json`，确认两条 job 的 payload 已成功写入目标内容。
- 已新增两份 shared 记录：
  - `memory/shared/2026-04-09_learning_ingest与Dream通知入口排查.md`
  - `memory/shared/2026-04-09_cron降噪与ingest升级落地记录.md`
- 已保留此前同轮产出：
  - `memory/shared/2026-04-09_learning_ingest_batch_升级设计稿_v1.md`

### 经验教训
- Dream 的噪音根因不是脚本 stdout，而是 cron prompt 自己要求“完成后回复 Dream完成”；真正降噪要改调度配置，不是靠口头要求模型少说话。
- `learning_ingest_batch` 当前最大问题不是模型不够强，而是交付结构仍停留在“摘要 + 手写 wiki 文件”时代；prompt 先扳正为结构化编译输入后，后续代码化才有意义。
- 外部运行配置与仓库代码是两套交付面：`~/.openclaw/cron/jobs.json` 负责立即生效，workspace 里的 shared / 日志负责审计与压缩恢复。两边都要落，不然下一轮容易失忆。

### 阻塞问题
- 这轮改动的是外部运行配置，不属于 workspace git 受管文件，无法像代码一样走常规提交；回滚要依赖手工备份文件。
- workspace 当前存在大量历史脏改动，不能盲目整体提交；只能精确提交本轮新增的记录文件。

### 厂长的指示
- 新增明确要求：不要做待命助手，要按工程协作者模式执行；任务内动作直接做，除非存在真实歧义，否则不问“要不要我继续”。
- 明确风格要求：结果汇报采用工程说明风格，做完一块报一块，不做过程性噪音汇报。

