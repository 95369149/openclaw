# Memory Index (L0)
> 每次启动只读这个文件 + task-board.json + 今日日志。需要详情再读 L1/L2。
> Last Updated: 2026-02-27 12:35

## 目录结构
| 目录 | 内容 | 优先级 |
|------|------|--------|
| 00_*.md | 大脑中枢 v5.3、数字分身 | P0 核心 |
| 01_强制规则/ | 安全红线、路由规则(排兵布阵v4.0)、碰撞规则、审批队列 | P0 永不删 |
| 02_知识库/ | 提示词系统、行业知识、技术文档 | P1 |
| 03_语义记忆/ | 学习笔记、分析报告、项目追踪 | P1 |
| 04_情景记忆/ | 对话记录、场景复盘 | P2 |
| 05_日常日志/ | 每日工作日志（30天后归档）| P1 |
| 06_工具配置/ | Perplexity 模型配置、工具设置 | P1 |
| 07_版本控制/ | 进化提案、版本日志 | P1 |
| 08_内容素材/ | 朋友圈、文案素材 | P2 |
| 10_项目/ | 进行中项目 | P1 |
| 20_领域/ | 领域知识（女儿教育等）| P1 |
| 80_收藏/ | X 解读、外部工具摘要 | P2 |
| 90_归档/ | 历史归档（只读）| P3 |
| scripts/ | 同步/监控/代理脚本 | P0 |
| shared/ | Agent 共享记忆（多 agent 通信）| P0 |
| ops/ | 运维状态（cron 写入，心跳读取）| P0 |
| templates/ | 模板文件 | P1 |

## Agent 编制（5员）

> ⚠️ 唯一权威源：`01_强制规则/排兵布阵.md` v5.0
> 以下为速查摘要，冲突时以排兵布阵为准。

| Agent | 模型 | 层级 | 定位 | 成本 |
|-------|------|------|------|------|
| jimmy | metaapi/gpt-5.3-codex | L0 | 调度中枢 + 浏览器 + 碰撞 | 💰 |
| deep | metaapi/gpt-5.3-codex | L1 | 中文/代码/批量（主力）| 💰 |
| logic | mynewapi/claude-sonnet-4-6 | L1 | 数学/推理 | 💰 |
| main | google-gemini-cli/gemini-3-pro-preview | L1 | 多模态/长文档 | 🆓 |
| kitt | mynewapi/claude-sonnet-4-6 | L2 | 架构/决策（慎用）| 💰 |

## 快速路由
- 厂长指令 → jimmy 判断意图桶（10桶）→ 派发对应 agent
- 不确定派谁 → 默认 deep（成本最低）
- 厂长指定 agent → 无条件服从
- 需要外部信息 → jimmy 自己碰撞（web_search/Perplexity）
- agent 失败 → 换 agent 重试一次，再失败报告厂长

## 公共记忆
- `shared/` — agent 完成任务后必须写记录
- `task-board.json` — jimmy 写入，所有 agent 可读
- `ops/state.json` — cron 写入，心跳读取
- **workspace 共享**：所有 agent 共享同一 workspace

## 记忆系统
- **QMD 三路混合搜索**：BM25 + 向量 + Reranking（memory.backend = qmd）
- **softThresholdTokens = 40000**：提前触发记忆刷新
- **sessionMemory = true**：历史会话可搜索
- **memorySearch.fallback = none**：强制本地检索

## 当前状态
- 活跃任务: 见 task-board.json
- 今日日志: 2026-MM-DD.md（根目录）
- 碰撞规则: 01_强制规则/外部碰撞规则.md
- 排兵布阵: 01_强制规则/排兵布阵.md（唯一权威源）

## 反思洞察

### 2026-02-26
1. **先测后改，不能闭眼动配置**：下午 provider 故障，我第一反应是改配置摘掉 xjrouter，被厂长批评。正确做法是先完整测试所有线路，拿到数据再决策。任何配置变更前必须跑一轮全量 provider 测试。
2. **子 agent 失败要立即降级到 jimmy 自己干**：今天 deep/main/kitt 三个全挂，但我花了太多时间在重试和等待上。应该第一次失败就启动降级，不超过2次重试。
3. **产品知识必须从源头核实**：之前所有文件都写成"木工设备"，直到厂长纠正才发现。教训：企业核心信息必须从官网/手册提取，不能凭记忆或推测。已建立 `红太阳企业信息档案.md` 作为唯一权威源。
4. **自我成长不能等厂长喂**：厂长发了4条X推文让我学习，批评我"自我成长机制不成熟"。已建立每日信息巡逻 cron（14:00），但还需要建立主动学习→消化→行动的闭环。
5. **Provider 冗余是生命线**：8条线路只有3条能用（mynewapi×2 + gemini flash），单点故障风险极高。厂长已在调研备用中转站（PoloAPI/灵芽/神马），需要尽快落地。
