# 关键配置变更模板

> 用途：凡是修改 `~/.openclaw/openclaw.json`、Gateway 启动项、provider/model 配置、agent 路由、启动关键脚本，都先按这个模板向厂长报备，再执行。
> 原则：先备份、讲风险、再修改、改后验活、必须可一键回滚。

---

## 1. 变更目标
- 本次准备修改：
- 修改原因：
- 预期收益：

## 2. 影响范围
- [ ] `~/.openclaw/openclaw.json`
- [ ] Gateway 启动 / 监听 / 端口
- [ ] provider / model 配置
- [ ] agent 路由 / 调度逻辑
- [ ] 启动关键脚本
- [ ] 其他：

## 3. 风险提示
- 可能风险 1：
- 可能风险 2：
- 最坏情况：Gateway 启动失败 / 模型不可用 / 消息通道异常

## 4. 变更前备份
### 备份文件
- 备份路径：
- 备份时间：

### 备份命令
```bash
cp <原文件> <原文件>.bak-时间戳
```

## 5. 计划改动（必须给厂长看）
### Diff / 变更摘要
```diff
<把准备修改的差异贴这里>
```

## 6. 执行后验活
### 必跑检查
```bash
openclaw gateway status
```

### 可选检查
```bash
openclaw status
```

### 验活结果
- Gateway：
- RPC probe：
- 当前会话：

## 7. 一键回滚
```bash
cp <备份文件> <原文件> && openclaw gateway restart
```

## 8. 执行结果记录
- 实际是否执行：
- 实际完成时间：
- 是否成功：
- 遗留问题：

---

## 使用规则
1. 没有备份，不准改。
2. 没有风险提示，不准改。
3. 没有厂长确认，不准改 `~/.openclaw/openclaw.json`。
4. 改后不验活，视为没完成。
5. 没给一键回滚命令，视为流程不合格。
