# 进化提案 2026-03-08

## 今日回顾

1. 截至凌晨 03:00，任务板处于清爽状态：进行中 0，待办 5，已完成 11，阻塞 0，说明系统执行面稳定，但新任务推进速度偏慢。
2. 最新共享记忆集中在三条主线：红太阳销售 Agent 场景设计、短视频 POV 模板沉淀、X 上对 AI Agent/审计链/内容流水线的工程化观察，方向整体一致。
3. 今日日志文件缺失，说明“跨日自动留痕”仍有空档；已补建今日日志并把本次巡检结果写入。

## 外链学习收获

1. 从今日英文结果看，2026 年 AI Agent 落地的主旋律不是“更自主”，而是“更可靠、可审计、能嵌进审批/采购/物流等固定流程”；对红太阳的启示是：优先做询盘分级、报价审查、售后诊断这类可衡量 ROI 的固定节点，而不是泛聊天机器人。
2. 英文结果同时反复强调 operational and repetitive tasks（审批、对账、采购、物流）是企业最容易先规模化采用 Agent 的区域；对红太阳的启示是：先从外贸询盘分拣、报价单红灯审计、配件补单/售后工单分流切入，再逐步延伸到设备远程诊断。
3. 中文搜索当天结果质量较差，更多是泛内容或二手转述，说明“制造业 AI 最新案例”这个主题需要后续升级为白名单信源巡检（如工信口、工业软件厂商、头部汽车/家电工厂案例），不能只靠通用搜索。

## 发现的问题和优化建议

### 高风险（需厂长确认）

1. **Agent 编制事实不一致**
   - `memory/01_强制规则/排兵布阵.md` 声称当前为 4 员编制（jimmy/main/deep/kitt）。
   - 但 `~/.openclaw/openclaw.json` 的 `agents.list` 仍保留 `logic`，实际是 5 个 agent 条目。
   - 同时，排兵布阵.md 写 jimmy 模型为 `geminiflash/gemini-3-flash-preview`，而配置中 jimmy 实际仍是 `mygptapi/gpt-5.4`。
   - 建议：由厂长确认“以文档收敛配置”还是“以配置回写文档”，然后做一次全量校准。

2. **SOUL.md 存在过时执行规则**
   - 规则“外链内容提取必须派 Scout 去读”已与现有 agent 编制不符，当前体系中并无 Scout。
   - 若继续保留，会在后续 cron/自动化任务里制造错误路由预期。
   - 建议：删除 Scout 相关遗留措辞，并把“外链由 jimmy 统一碰撞/抓取”的现行机制写死。

### 中风险（建议近期处理）

3. **SOUL.md 存在重复规则**
   - “配置变更必须全量同步”第 8 条重复出现两次。
   - 建议：下次维护时去重，减少规则噪音。

4. **工具调用不超过 3 次/轮 的规则已不贴合现状**
   - 当前强制读记忆、读技能、巡检状态、本地文档优先等机制叠加后，3 次上限经常天然失效。
   - 建议：改为“默认少调用，复杂任务允许突破，但要并行和压缩步骤”。

5. **ops/state.json 的 agentStats 仍保留 logic 统计口径**
   - 当前 weekly/daily 自检里仍能看到 logic 字段，说明监控口径也未与文档彻底校准。
   - 建议：后续统一 agent roster 后一并修正统计字段。

### 低风险（可自动实施 / 已实施）

6. **今日日志缺失** → 已自动补建 `memory/2026-03-08.md`。
7. **memory/.abstract 日期与索引指向滞后** → 已自动更新 Last Updated、今日日志、最新进化提案。

## 对红太阳数控的具体启示

1. 把 AI 先塞进“固定动作链”而不是“开放式助手”：
   - 询盘分级 Agent
   - 报价风险审计 Agent
   - 售后工单/远程诊断分流 Agent
2. 把设备智能化卖点从“机器参数”升级成“可持续服务能力”：
   - 设备数据采集
   - 故障预警
   - 配件推荐
   - 远程诊断与复盘
3. 内容侧继续坚持“人味 + 场景 + 设备能力顺手带出”的短视频路线，不要回到生硬宣传片。

## 风险等级汇总

- **高风险需厂长确认**：Agent 编制与配置不一致；SOUL.md 中 Scout 遗留规则。
- **中风险建议近期处理**：SOUL 重复规则、3 次工具上限、agentStats 口径未校准。
- **低风险可自动实施**：补建今日日志、更新 .abstract 索引（已完成）。

## 建议的下一步

1. 厂长确认 Agent 真相源：保留 logic 还是彻底移除。
2. 确认后一次性同步 `openclaw.json` / `排兵布阵.md` / `.abstract` / 统计口径。
3. 为“制造业 AI 案例”建立固定信源清单，替代低质量通用搜索。
