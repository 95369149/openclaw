# 心跳自动响应规则

## 触发条件

当收到包含"Kitt心跳检测"的WhatsApp消息时，自动更新心跳时间戳。

## 实现方式

在SOUL.md或强制规则中增加：

- 检测到"Kitt心跳检测"关键词
- 立即执行：`date +%s > ~/.openclaw/.last_heartbeat_pong`
- 回复：`PONG ✅`（可选，避免刷屏可以不回复）

## 注意

- 这是**被动响应**，不需要我主动发消息
- 只要我能处理消息，就能更新时间戳
- 如果我卡死无法处理消息，时间戳不更新，触发重启
