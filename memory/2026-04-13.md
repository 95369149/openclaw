# 2026-04-13 日志

## 关键决策

### memory/ 目录 git 同步策略
- 厂长授权将 memory/ 推送到 GitHub（2026-04-12 22:40）
- 执行后推送成功（908 files，commit: 6b8f2541c）
- 厂长随后决定**暂缓推送记忆文件**，需将 memory/ 重新加回 .gitignore
- 原因：memory/ 包含系统内部敏感信息，不适合放 GitHub

### .gitignore 状态
- 当前 memory/ 已被推送到远端（commit 6b8f2541c）
- 待执行：将 memory/ 重新加回 .gitignore，防止后续日志继续推送
- IDENTITY.md 和 USER.md 已从 .gitignore 移除（永久授权）

## 状态变更

### daily_git_backup cron 修复
- cron job id: `fb935739-0d09-4e57-b2eb-80042ef2a4ba`
- 问题：IDENTITY.md 和 USER.md 被 .gitignore 忽略导致备份失败
- 修复：git add 改为 `git add -f IDENTITY.md USER.md`
- 已补跑验证通过

### memory/ 知识库内容摘要（厂长询问）
- 系统运行类：强制规则、大脑中枢、数字分身
- 知识积累类：学习笔记、wiki、女儿教育
- 业务类：项目文档、生产SOP、视频工厂素材
- 日志类：每日日志、shared任务记录、情景记忆
- 归档类：历史版本和旧规则

## 待办

- [ ] 将 memory/ 重新加回 .gitignore（厂长指示暂缓推送）
- [ ] 确认 .gitignore 修改后推送，防止后续 memory 文件继续进入 git 历史


---

## 2026-04-13 晚间更新（21:00-22:00）

### bridge 仓库大规模同步完成
- 厂长指示将 Mac 上分散的记忆系统、Obsidian 笔记、售后文档、项目资料同步到 `hty-hermes-bridge`
- 扫描并归档了 1254 个文件，按主题分类：
  - `second_brain/raw/`：WSCAD BOM、企业文化、销售培训、标书、步国军生产管理
  - `second_brain/raw/历史导入/kitt_memory/`：965 个 memory 文件
  - `knowledge/aftersales/raw/`：售后原始记录
  - `second_brain/inbox/`：待整理任务
- 生成清单 `second_brain/SYNC_MANIFEST_2026-04-13.md`
- 由于文件数量大，push 过程多次被 SIGKILL，最终通过分批提交 + auto sync cron 完成
- 远端总文件数：1342，最新 commit: `ca8e293`

### skills root 越界告警修复
- 问题：`workspace/skills/` 下有 66 个 symlink 指向 `../.agents/skills/xxx`，gateway 启动时告警 `Skipping skill path that resolves outside its configured root`
- 修复：删除 66 个 symlink，将 `~/.agents/skills/` 下全部内容复制到 `~/.openclaw/workspace/skills/`
- 修复后 workspace/skills/ 共 95 个目录，0 个 symlink
- gateway 已重启验证，告警消失

### memory/ GitHub 同步策略最终状态
- 已将 memory/ 重新加回 `.gitignore`
- 从远端撤回 memory/ 文件（commit `7e18149b8`，938 files deleted）
- 当前策略：memory/ 本地保留，但不再推送到 GitHub

### 系统稳定性修复
- `gateway-guardian.sh` 健康检查：将 `openclaw gateway status` 替换为 `curl http://127.0.0.1:18789/` HTTP 探针，避免检测逻辑自身卡死
- Discord 通道：确认 WebSocket 断连是代理层长连接问题，gateway 进程未崩溃；厂长要求保留作为资料储存库，已重新启用

### Hermes bridge 双向流转验证
- Windows Hermes 生成的 `pricing_table_2026-04-13.csv`（55 条记录，总价 3,260）Mac 侧通过 `git pull --rebase` 成功拉取
- 售后知识库 v1 同步完成：17 个故障分类 + `_manifest.json`，已复制到本地 `memory/02_知识库/售后知识库/`
- 推送二次分类任务 `inbox/reclassify_untagged.md`，定义 12 个新增子标签，待 Windows Hermes 处理

### 待办更新
- [x] bridge 仓库大规模同步
- [x] skills root 越界告警修复
- [x] memory/ 重新加回 .gitignore 并撤回远端
- [x] gateway 健康检查修复
- [x] Discord 重新启用
- [ ] Windows Hermes 处理 `inbox/reclassify_untagged.md`
- [ ] qmd 独立健康监控文件（`memory/ops/qmd-status.json`）
- [ ] guardian 脚本拆分为 3 层
