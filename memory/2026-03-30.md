
## 18:44 Flush
### 1) 关键决策和原因
- 为补齐“路由模式只靠提示词、容易失效”的短板，落地了最小可行代码方案：新增 `/Users/apple/.openclaw/workspace/bin/router.py`，用关键词/意图桶将任务分流到 `jimmy/scout/deep/kitt/guard/main`。原因：厂长明确要求“把短板补齐，只要结果”，且当前多 Agent 协作缺少硬约束。
- 为让厂长可直接用编程软件接管抖音项目，确认优先接入 Claude Code 到 `dyproject`，并配置为 Sonnet 模型。原因：抖音项目已有 `CLAUDE.md`，比外贸拓客系统更适合直接接入。
- 选择 `index.html` 作为抖音前端主版本，不切回 `index_new.html` / `index.v2.raw.html`。原因：`index.html` 功能更完整，是合并版，当前后端也跑这个版本。

### 2) 状态变更
- 已写入文件：`/Users/apple/.openclaw/workspace/bin/router.py`。
- 已为 Claude Code 安装全局包：`@anthropic-ai/claude-code@2.1.87`。
- 已创建配置文件：`/Users/apple/.claude/settings.json`，并设为 Sonnet（`claude-sonnet-4-6`）。
- 用户已成功进入 Claude Code 首次启动界面，截图显示当时并非报错，而是主题选择页。
- `learning_ingest_batch` 后续状态显示：`memory/inbox/` 为空，因此 ingest 无内容可处理，并已记录到 `memory/shared/2026-03-30_learning_ingest.md`。

### 3) 经验教训
- 路由规则只写在 SOUL/AGENTS 里不够，必须逐步机械化到代码或 hook；否则上下文压缩后极易退化成“自己硬做 + 频繁追问”。
- 用户明确偏好“你自己判断、我只要结果”，遇到清晰任务应直接产出，不要再反复确认。
- 给用户配置本地开发工具时，要先确认是否只是首次启动向导，不要把非报错界面误判为故障。
- 对 cron 模型切换类故障，不能口头说“修好了”；要以实际 run 成功或错误清空为准。

### 4) 阻塞问题
- `learning_ingest_batch` / `daily_learning` / `daily_learning_intake` 的 `LiveSessionModelSwitchError` 仍未彻底根治。虽然 job 列表显示模型字段为 `google-gemini/gem...`，但错误仍引用 `mynewapi/claude-sonnet-4-6`，说明冲突源不一定只在 job 的 `model` 字段，可能在 payload/message/会话继承链。
- Claude Code 虽已配置可启动，但尚未验证真实 API 调用是否成功完成一次任务。

### 5) 厂长的指示
- “那就先把这个短板补齐” —— 指补齐多 Agent 路由/分流的硬约束短板。
- “你自己判断” —— 不要再把决策抛回给厂长。
- “你落地好就行，我只要结果” —— 以后汇报以结果为主，少讲过程。
- “你给我装好” / “还需要配置，你去配一下” / “用sonnet” —— 已落实 Claude Code 安装与 Sonnet 配置。
