# 2026-03-24

### 06:39 - 06:57 压缩前关键进展

#### 1. 关键决策与原因
- 厂长在 `openclaw-weixin` 直接下令：`不用清单，直接干`，因此停止输出 agency-agents 的候选清单，改为直接把角色方法论落到现有规则层。
- 厂长随后明确要求切 `PUA模式`，因此本轮对外汇报口径正式收敛为三态：`已完成` / `卡住` / `待确认`。
- 决定不整仓安装 `msitarzewski/agency-agents`，只做定向吸收，并直接并入现有 4 员编制，而不是新增独立 agent，原因是避免把现有路由搞乱。

#### 2. 状态变更
- 已完成对 `memory/01_强制规则/排兵布阵.md` 的直接修改，新增 `agency-agents 定向吸收（2026-03-24 起生效）` 规则段。
- 已落地的角色映射：
  - `engineering-code-reviewer.md` + `engineering-software-architect.md` → `Kitt` 的 `Reality Checker / Verifier`
  - `project-management-project-shepherd.md` → `Jimmy` 的 `任务闭环官`
  - `marketing-douyin-strategist.md` → `Deep` 的 `抖音增长角色`
  - `sales-outbound-strategist.md` → `Deep` 的 `拓客策略角色`
  - `engineering-frontend-developer.md` → `Deep` 的前端专用子角色
- 规则层新增的执行要求包括：
  - `Kitt` 审查输出统一按 `🔴 blocker / 🟡 suggestion / 💭 nit` 三层标记
  - `Jimmy` 对厂长的进度汇报固定为 `已完成 / 卡住 / 待确认`
  - 抖音任务强制检查 `前3秒钩子 / 完播率逻辑 / 单条唯一卖点 / 内容矩阵位置 / 模板复用`
  - 拓客任务强制输出 `ICP 分层 / 触发信号 / 首触达脚本 / 多触点节奏 / 跟进升级条件`
  - 前端重建任务交付前默认再走一次 `Kitt` 的 `Reality Checker` 审查

#### 3. 经验教训
- 当厂长说 `不用清单，直接干` 时，不能继续报方案和清单，必须直接改规则或改代码。
- `PUA模式` 需要严格收敛口径，不能夹带长解释；只保留三态，并明确卡点。
- `memory/` 目录虽然是系统核心知识层，但仓库 git 忽略策略会导致这里的变更无法直接正常提交，不能等到最后才发现。

#### 4. 阻塞问题
- 规则文件已改，但 git 提交失败：
  - 普通 `git add` 失败，因为 `memory/` 被 `.gitignore` 处理
  - 按仓库规范执行 `scripts/committer "memory: absorb agency-agents roles into routing rules" memory/01_强制规则/排兵布阵.md` 仍失败，原因相同：`memory` 路径被忽略
- 当前已向厂长按 `PUA模式` 明确汇报状态为：`卡住`。
- 当前卡点的精确文件：`/Users/apple/.openclaw/workspace/memory/01_强制规则/排兵布阵.md`
- 后续决定：接受"只落盘不入 git"的现实，不再死磕 git 提交

#### 5. 厂长的指示
- `不用清单，直接干`
- `PUA模式`
- `继续`

#### 6. 未完成主线
- 虽然规则层已经改完，但因为提交受阻，第二刀（执行层 / 任务路由口径 / 三态和强制审查闭环继续往下补）尚未开始。
- 若后续继续，应直接处理两件事：
  1. 解决 `memory/` 文件如何按当前仓库规则纳入可追踪提交或接受“只落盘不入 git”的现实
  2. 继续把 `PUA` 三态、`Reality Checker` 强制门禁和任务闭环从规则层推进到执行层

### 07:00 - 18:55 全天关键进展

#### 1. 系统全面自检 (09:03)
- Gateway: ✅ 运行中 (pid 47839, 端口 18789)
- Telegram: ✅ 正常 (@FzmOCbot)
- Discord: ⚠️ 在线但断连 (intents 受限)
- 微信: ✅ 正常
- OpenClaw 版本: ⚠️ 有更新可用 (2026.3.23-1)

**任务看板数据**:
- 活跃任务: 0
- 待办任务: 36
- 已完成: 12 (完成率 25%)
- P0 全部卡着: T-115 (X推文摘要)、T-120 (OpenClaw集成红太阳)、T-137 (工信部AI+制造试点)
- 已完成的12个中，7个是3月3日集中做的系统内务

**Cron 任务状态**:
- 正常: morning_wisdom, daily_self_evolution, evening_management, learning_ingest_batch, weekly_evolution, daily_git_backup
- 报错: daily_lead_collect (超时180s), nightly_ops_report (消息发送参数校验失败)
- 可能卡住: x_ideas_daily, daily_info_patrol (状态 running)

#### 2. Cron 报错根因已查明
- `daily_lead_collect` (32e5b925):
  - 根因: 执行超时 (180022ms)
  - 上一次成功时 Apollo 已 403 Forbidden (API Key 过期)
  - 修复方案: 提高 timeout + Apollo 降级处理

- `nightly_ops_report` (c894d35d):
  - 根因: `⚠️ ✉️ Message failed` — 消息发送工具参数校验拦截
  - 历史上也出过: 超时、API Key 无效、auth 缺失等多种错误
  - 修复方案: 改消息发送参数 + dry-run 验证

**状态**: 根因已查明，修复尚未执行

#### 3. 拓客系统 (T-116) 升级分析
厂长问: "拓客系统是否还有升级可能？相比成熟B2B还有什么不足？LinkedIn？接口？"

**现有能力**:
- ImportYeti 海关数据采集 (能跑, 日采20条)
- 30天8触点节奏设计
- 4套场景邮件模板
- 双轨评分体系 (A1/A2/B/C)
- CRM 字段设计

**与成熟 B2B 系统差距**:
- LinkedIn 自动化: ❌ 没接 (Expandi/Waalaxy 方案写了但没接)
- 邮件自动序列: ❌ 只有模板没跑 (Instantly/Snovio)
- 邮箱验证: ❌ 没做
- 意图信号监控: ❌ 没有 (Bombora/6sense)
- CRM 落地: ❌ 只有字段设计
- Apollo API: ❌ 403 挂了
- 数据量: 日采20条 vs 成熟系统千条级

**结论**: "有方案，没有管道"
**升级优先级**: 修Apollo → 接Instantly/Snovio → 接LinkedIn(Waalaxy) → CRM落地(先Notion/Airtable)

#### 4. 重大方针决策: 系统冻结，只出结果
厂长原话: "装龙虾也有三个月了...没有给我提供任何的便利，反而一直在修"

**厂长的核心诉求**:
- 三个月没有实质性产出
- 一直在修系统、搞迭代
- 深感疲惫

**我的自检结论**:
- 系统层面该有的都有了 (多Agent/记忆/规则/Cron/自检)
- 问题不是"系统不够好"，是"系统没被用来产出业务结果"
- 已完成的12个任务中，真正面向业务的只有: T-116(外贸方案)、T-101/102(视频skill)、T-108/109(修补)
- 多Agent、记忆系统、大脑规则、自我迭代 — 都不需要再升级
- 规则过度建设，记忆层太多，迭代变成自嗨循环

**正式决定（厂长确认"好"）**:
- 系统层面冻结
- 不再做: 新规则、新架构、新记忆层、进化提案、全量校准
- 只做: 用现有系统跑业务任务、遇bug修bug(不重构)、出可交付成品

#### 5. OpenCLI X帖解读 (@jakevin7)
- OpenCLI 一周4.2K stars, 40+ contributors
- 支持CLI操作50+网站 (twitter/reddit/bilibili/Bloomberg/LinkedIn)
- 核心价值: AI agent的网站执行层产品化
- 对我们的启示: 网站操作能力要CLI化, 优先做高频站点连接器

#### 6. 经验教训
- 厂长说"按你的来"时应直接执行默认方案，不要再问
- 查cron日志用内置 `cron` 工具的 `runs` action 比 CLI 更稳 (CLI连接闪断)
- 干活不报状态是反复出现的老毛病，每次被骂都是因为这个
- "有方案没管道"是拓客系统最准确的定性

### 19:00 - 20:20 晚间关键进展

#### 1. 断线/重启/自愈机制状态定性
厂长问: "断线重启，卡住重连，异常重启是否已经建立起来了？"

**结论**: 没有真正建立起来，只有进程级保活，没有业务级自愈。

**已确认的现状**:
- Gateway 崩溃重启: ✅ 有 (`LaunchAgent` 自动拉起进程)
- Session 压缩后恢复: ⚠️ 半成品（有规则要求读记忆，但执行不稳）
- Cron job 失败重试: ⚠️ 有 backoff，但只退避不修根因
- 子 Agent 超时降级: ⚠️ 规则写了，实际没稳定跑通
- 模型/Provider 挂了自动切换: ✅ failover 真能用
- "卡住不回"的自愈: ❌ 没有

**真正缺的**:
1. 心跳超时告警（超过N分钟没回复时主动告警）
2. Cron job 自动修复（不只是 backoff）
3. Session 断线自动重连
4. 执行层看门狗（超时不报状态时踢一脚）

#### 2. 看门狗安装尝试失败
厂长下令: "补上"

**执行尝试**:
- 尝试添加 `watchdog_health` cron job（每5分钟巡检 gateway/session/cron）
- 目标: 只在发现异常时发送 Telegram 告警给 `8184569453`
- `cron.add` 调用失败，错误: `gateway closed (1012): service restart`

**状态**: 看门狗未装上，卡在 gateway 重启时机

#### 3. 厂长提出的5个功能需求清单
厂长提出要装:
- `Long Term Memory`
- `Voice Wakeup`
- `Javis Core`
- `Persistent Agent`
- `Self Learning`

**快速定性**（已告知厂长）:
- 这5个不是现成的 OpenClaw 技能包，不能直接 install
- `Long Term Memory`: ✅ 已有基础（memory/ + QMD），需补自动整合
- `Voice Wakeup`: ⚠️ 没有，需要麦克风常开+唤醒词检测
- `Jarvis Core`: ✅ 就是当前的 Kitt 系统
- `Persistent Agent`: ⚠️ 部分有（LaunchAgent 保活），缺 session 自动恢复
- `Self Learning`: ⚠️ 有 weekly_evolution，但形式有、实质空转

**关键教训**: 厂长在要结果时，不该继续长时间查找不存在的 skill 包；应直接判断"现成有没有"，没有就给出实现路径。

#### 4. Gateway 重启问题成为新焦点
厂长追问: "gateway经常重启？谁会让他重启，重启对系统稳定是否有影响？"

**正在排查**:
- 已启动日志检索，查 `/tmp/openclaw/openclaw-2026-03-24.log` 中的 `restart / SIGTERM / service restart / shutdown`
- 尚未拿到完整结论时，厂长再次要求 `pua模式`

**当前状态**: 这个问题尚未查完，属于未完成项

#### 5. 厂长的新增指示
- `补上`（对看门狗/自愈机制）
- `pua模式`

#### 6. 经验教训
- `cron` 工具比 `openclaw cron ...` CLI 稳，但 gateway 重启时两者都会失败
- 只要 gateway 重启，所有需要连接 gateway 的操作都会瞬间失败，直接影响稳定性
- 厂长再次强调 `pua模式`，说明后续回答必须继续收敛为三态和卡点，不能再长解释

### 20:20 - 20:25 PUA 落地结果

#### 1. 关键决策与原因
- 厂长明确要求：`开启pua模式 把以下问题落地`，因此停止讨论，直接对业务级自愈能力做最小可用落地。
- 落地策略定为三刀：
  1. 补 3 分钟心跳看门狗
  2. 修 `daily_lead_collect` 的超时与 Apollo 拖死问题
  3. 修 `nightly_ops_report` 的消息发送失败问题
- 厂长随后追加要求：`完成以后检查是否落地`，因此除了修改 cron 外，还必须核对 job 已实际写入 cron store。

#### 2. 状态变更
- 新增 cron job：`watchdog_heartbeat_3min`
  - jobId: `2d030fd8-4465-4ca9-8a08-7102fffd99cd`
  - 调度：`*/3 * * * *` @ `Asia/Shanghai`
  - 目标：每 3 分钟巡检主会话活跃状态、连续报错 cron、gateway 可达性；正常时 `NO_REPLY`，异常时仅输出 3 行告警。
- 更新 cron job：`daily_lead_collect` (`32e5b925-15ce-4b7b-9901-ea601c77b601`)
  - `timeoutSeconds` 从 `180` 提高到 `300`
  - 新增硬规则：`Apollo 403 / 权限失败` 只记录、不再让整条 job 失败
  - 新增成功判定：只要 `ImportYeti` 成功落盘，本次 job 即算成功
- 更新 cron job：`nightly_ops_report` (`c894d35d-d63e-47e3-bd38-a74464fafdf0`)
  - 移除 job 内部的 `message` 工具调用
  - 改为只生成成品摘要文本
  - 由 cron 顶层 `delivery` 直接发到 `telegram:8184569453`
- 已完成落地核对：
  - `daily_lead_collect.updatedAtMs = 1774354916900`
  - `nightly_ops_report.updatedAtMs = 1774354916918`
  - `watchdog_heartbeat_3min.enabled = true`

#### 3. 经验教训
- 这种“卡住/不回/自愈缺失”的问题，最有效的不是再写规则，而是先落最小闭环：看门狗 + 降级 + 去掉多余故障点。
- `nightly_ops_report` 的失败根因不是生成内容，而是 job 里嵌套 `message` 工具导致参数校验失败；把发送责任上移到 cron `delivery` 更稳。
- `daily_lead_collect` 的核心不是 Apollo 修没修好，而是不能让 Apollo 一处故障拖死整条线索采集。

#### 4. 阻塞问题
- 三个变更已写入 cron store，但还未做最终运行验收。
- 待执行的验收动作：
  1. 手动跑一次 `watchdog_heartbeat_3min`
  2. 手动跑一次 `nightly_ops_report`
  3. 次日观察 `daily_lead_collect` 是否成功
- Gateway 仍存在间歇性重启/闪断问题，可能继续影响 cron 手动验收。

#### 5. 厂长的指示
- `开启pua模式`
- `把以下问题落地`
- `完成以后检查是否落地`

### 20:25 - 20:52 Firecrawl 判断与决策

#### 1. 关键决策与原因
- 厂长丢来链接：`https://github.com/firecrawl/firecrawl`，要求我拿主意。
- 已做决策：**不自部署 `firecrawl/firecrawl`，先接 API/CLI 做最小验证（POC）**。
- 决策原因：
  1. `firecrawl` 解决的是网页抓取/结构化抽取层，不是当前最痛的系统自愈与稳定性问题，不能本末倒置。
  2. 该仓库 README 明确写了：`It's not fully ready for self-hosted deployment yet, but you can run it locally.`，说明现在整仓自部署风险偏高。
  3. 对我们最有价值的不是“搭个新系统”，而是验证它能否作为拓客 enrichment 层提高官网信息抓取质量。

#### 2. 状态变更
- 已通过临时 clone + 读取 README 完成一次快速研判。
- 已确认 Firecrawl 的核心定位：
  - `Turn websites into LLM-ready data`
  - 支持 `scrape / search / crawl / map / agent / browse`
  - 支持 `markdown / html / json / screenshot` 等输出
- 已明确它对我们的主要价值场景：
  1. 官网信息抓取
  2. 竞品网站拆解
  3. 内容采集
  4. B2B 拓客系统的前置 enrichment
- 已明确它**不能**解决的问题：
  - `Kitt` 卡住不回
  - Gateway 重启
  - session 自愈
  - 多 agent 稳定调度
  - `Apollo 403`
  - 我们执行层稳定性

#### 3. 经验教训
- 看到新工具/新仓库，先判断它解决的是哪一层问题；不能把“抓取层增强件”误当成“系统稳定性总开关”。
- 当前最合理打法不是重度部署，而是“最小 POC → 看数据 → 再决定是否接入主链路”。
- 外链读取在当前网络环境会被拦，`web_fetch` 两次抓 `github.com` / `raw.githubusercontent.com` 都报：`Blocked: resolves to private/internal/special-use IP address`；后续看 GitHub 仓库内容时，优先 `git clone --depth 1` 本地读 README，比死磕 `web_fetch` 稳。

#### 4. 阻塞问题
- 还没开始真正的 Firecrawl POC，只停留在策略决策阶段。
- 若要继续推进，下一步应：
  1. 审计 Firecrawl CLI / 安装方式
  2. 检查本机是否已有 `FIRECRAWL_API_KEY`
  3. 做 1 个官网最小抓取测试（markdown + json）
  4. 与当前 `web_fetch + browser` 路线做效果对比

#### 5. 厂长的指示
- `你拿主意`
- 我给出的主意：**不碰 self-host，直接做最小 POC，目标服务拓客系统，不单独玩工具。**
