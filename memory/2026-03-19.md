## 07:52 拓客系统落地推进

### 关键决策和原因
- 厂长要求继续把 `projects/b2b-growth-system` 落地，不停留在方案层，直接往“可运行的最小闭环”推进。
- 现场代码盘点后确认：项目并非从零开始，后端 API 和前端主要页面骨架已基本存在，因此策略调整为“补缺口+跑通闭环”，而不是重搭一套。
- 当前优先级明确为：
  1. 修复前后端类型/接口断点；
  2. 补全缺失页面与全局商机接口；
  3. 增加一键启动方式；
  4. 再做真实数据导入与验证。

### 状态变更
- 已确认 `T-116` 虽在 `task-board.json` 中标记为 done，但实际状态更接近“已搭骨架，未完全跑通”。
- 已完成代码摸底：
  - 后端已有账户、日志、阶段、提醒、导入 CSV、线索来源、商机录入、AI 打分、AI 草稿等接口；
  - 前端已有 dashboard / accounts / tasks / sources / cadence / account detail / new account 页面；
  - `opportunities` 页面依赖全局 `/api/opportunities` 接口，但后端尚未补出该接口；
  - `Dashboard` 已返回 `stage_counts`，但前端尚未渲染漏斗；
  - `frontend/lib/types.ts` 缺 `stage/source_platform/source_batch` 字段。
- 已实际修改：
  - `projects/b2b-growth-system/frontend/lib/types.ts` 已补入 `stage`、`source_platform`、`source_batch` 字段。

### 经验教训
- 这类“继续落地”任务，第一步必须先做代码资产盘点；否则容易误判成空项目，重复造轮子。
- `task-board` 的 done 状态不能盲信，必须以代码与运行结果为准。
- 项目现阶段最大的短板不是业务逻辑，而是“最后 20% 的打通工作”：类型、接口、启动脚本、真实运行验证。

### 阻塞问题
- 后端依赖未验证是否完整安装，系统尚未实测启动。
- `backend/.env` 中发现明文 `OPENAI_API_KEY`，属于安全风险；后续如继续推进，需优先脱敏到环境变量引用。
- `opportunities` 页面目前会依赖不存在的全局接口，未补完前页面不可完全工作。

### 厂长的指示
- 明确指示：`继续把拓客系统落地`。

### 13:41 拓客系统采集层重构确认

#### 关键决策和原因
- 厂长严厉指出当前系统只是个“线索整理器”（依赖手动导入CSV分类），而不是真正的“线索采集器”，缺乏实际业务价值。
- **决策**：必须重构采集层，引入真实的外部源（Source Connector），实现“外部真源抓取 → 去重 → ICP打分 → 联系人补全 → 自动触达”。
- **下一步行动**：停止盲目写代码，先使用 Perplexity 等工具进行外部碰撞，调研 ImportYeti、Apollo、Snovio 等真实数据源 API 的接入方式和免费额度，输出切实可行的 Source Connector 技术方案。

#### 状态变更
- 经代码核实：当前后端没有接入任何真实采集源，前端 `sources/page.tsx` 为空壳，一切线索录入均依赖 CSV 手动导入。

#### 经验教训
- 面对质疑和需求，必须第一时间读取历史日志（05_日常日志）和方案文档（shared/），不能脱离上下文瞎猜。
- 业务系统必须闭环到“源头获取”，没有真实活水（外部采集能力）的 CRM 骨架对厂长来说没有意义。

#### 阻塞问题
- 尚缺各真实线索源（如 ImportYeti、Apollo）的 API 文档、接入方式及成本评估（需外部碰撞解决）。

#### 厂长的指示
- 拒绝形式主义的二次分类，必须接入真正的链接采集源。
- 不懂就去碰撞拿真实结果，绝不允许闭门造车、瞎写胡乱堆叠的垃圾代码。

## 18:28 新 Session 启动 + 配置变更 + 抖音项目

### 关键决策和原因
1. **Kitt 模型锁定 Opus 4.6**：厂长要求 kitt agent 固定使用 opus，不允许 fallback 轮换。已通过 `config.patch` 将 kitt 的 model 改为 `{ primary: "mynewapi/claude-opus-4-6", fallbacks: [] }`，gateway 已重启生效。
2. **抖音项目是升级不是新建**：厂长明确指出抖音项目已有完整底子（P02 视频推广计划、P06 谷歌一条龙方案、日更选题提示词、已出样片），要求的是升级现有系统，不是从零规划。

### 状态变更
- `openclaw.json` agents.list 中 kitt 的 model 从字符串 `"mynewapi/claude-opus-4-6"` 改为对象 `{ primary: "mynewapi/claude-opus-4-6", fallbacks: [] }`
- Discord 状态探测正常：enabled, configured, running, connected

### 经验教训
1. **读记忆再开口**：抖音项目已有大量资产（P02、P06、选题提示词、样片），但 memory_search 全部返回空（QMD 搜索未命中），导致我误判为新项目从零规划，浪费厂长时间。教训：QMD 搜索不到不代表没有，必须用 `find + grep` 兜底搜文件系统。
2. **升级 vs 新建的判断**：厂长说"抖音项目"时，应该先确认现有资产状态，而不是直接输出新方案。这是第二次犯"不读记忆就开口"的错误。

### 现有抖音项目资产清单
- `memory/10_项目/P02_视频推广计划.md` — TikTok/YouTube 海外短视频完整方案（10个脚本模板、30天排期、标签策略）
- `memory/10_项目/P06_谷歌一条龙_全API自动化量产方案.md` — 全 API 自动化量产（Gemini+Imagen+Veo+Lyria，$4.72/条，90条/月）
- `memory/shared/2026-03-14_jimmy_短视频选题.md` — 最新选题+Veo/Imagen提示词
- `memory/shared/2026-03-12_jimmy_daily_video_topics.md` — 日更选题
- `memory/shared/2026-03-11_短视频生成提示词_AI_Studio.md` — AI Studio 直接可用的提示词库
- `memory/02_知识库/视频制作提示词模板.md` — 提示词模板
- `memory/90_视频工厂/` — 视频工厂资产目录
- `output/video_20260312_选题1_振动刀精度.mp4` — 已出样片
- `skills/video-factory/SKILL.md` — 视频工厂 skill
- `memory/insights/content-sop.md` — 内容生成 SOP

### 待办
- 抖音项目升级方案尚未开始，需基于现有资产做升级而非重建
- Discord 连接问题：探测正常但厂长反馈连不上，需进一步确认具体症状

### 厂长的指示
- kitt 固定 opus 不轮换
- 抖音项目要升级，不是新建，要求"专业架构师水平，想到使用者想不到的地方，不做四不像"
- 先查 Discord 连接问题
